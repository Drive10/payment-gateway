package dev.payment.common.config;

import org.springframework.context.annotation.Configuration;

/**
 * Standard logging configuration for all PayFlow services.
 * This ensures consistent logging format and levels across services.
 */
@Configuration
public class LoggingConfiguration {
    // Logging configuration can be added here if needed
    // For now, this serves as a placeholder to demonstrate the pattern
}