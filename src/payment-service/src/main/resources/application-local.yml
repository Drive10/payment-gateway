spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/payflow
    username: payflow
    password: payflow
  data:
    redis:
      host: localhost
      port: 6379
      password: payflow
  kafka:
    bootstrap-servers: localhost:9092

services:
  auth-service:
    url: http://localhost:8082

server:
  port: 8083
