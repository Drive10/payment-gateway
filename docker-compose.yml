services:
  frontend:
    image: nginx:alpine
    ports:
      - "3000:80"
    volumes:
      - ./web/frontend/dist:/usr/share/nginx/html:ro
    networks:
      - payflow-network

networks:
  payflow-network:
    driver: bridge