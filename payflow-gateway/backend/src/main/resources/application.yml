server:
  port: 8082

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: payflow_gateway

management:
  endpoints:
    web:
      exposure:
        include: health,info
