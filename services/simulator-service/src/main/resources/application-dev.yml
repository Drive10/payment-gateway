spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:postgresql://localhost:5433/simulatordb
    username: payment
    password: paymentpass

  jpa:
    hibernate:
      ddl-auto: validate