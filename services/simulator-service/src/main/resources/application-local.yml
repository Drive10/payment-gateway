spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/${DB_NAME:simulatordb}
    username: ${DB_USERNAME:${SIMULATOR_DB_USER:simulator}}
    password: ${DB_PASSWORD:${SIMULATOR_DB_PASSWORD:simulatorpass}}
  flyway:
    validate-on-migrate: false
