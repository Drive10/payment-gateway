spring:
  application:
    name: simulator-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  flyway:
    enabled: false

server:
  port: ${SERVER_PORT:8086}

logging:
  level:
    root: INFO
    dev.payment: DEBUG