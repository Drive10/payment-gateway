spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/paymentdb
    username: payment
    password: paymentpass

  kafka:
    bootstrap-servers: kafka:29092

application:
  simulator:
    base-url: ${SIMULATOR_BASE_URL:http://simulator-service:8085}

  gateway-trust:
    enabled: true

logging:
  level:
    root: INFO
