spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:postgresql://localhost:5433/riskdb
    username: payment
    password: paymentpass

  jpa:
    hibernate:
      ddl-auto: validate