spring:
  datasource:
    url: ${LEDGER_DB_URL:jdbc:postgresql://postgres:5432/ledgerdb}
    username: ${DB_USERNAME:payment}
    password: ${DB_PASSWORD:paymentpass}
  jpa:
    hibernate:
      ddl-auto: validate
