spring:
  datasource:
    url: ${SETTLEMENT_DB_URL:jdbc:postgresql://postgres:5432/settlementdb}
    username: ${DB_USERNAME:payment}
    password: ${DB_PASSWORD:paymentpass}
  jpa:
    hibernate:
      ddl-auto: update
