spring:
  config:
    activate:
      on-profile: dev

  cloud:
    gateway:
      routes:
        - id: payment-auth
          uri: http://localhost:8084
          predicates:
            - Path=/api/v1/auth/**

        - id: auth-service
          uri: http://localhost:8084
          predicates:
            - Path=/platform/auth/**