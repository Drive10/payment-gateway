spring:
  config:
    activate:
      on-profile: hybrid
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://host.docker.internal:27017/audit_db}
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:host.docker.internal:9092}

server:
  port: ${SERVER_PORT:8090}
