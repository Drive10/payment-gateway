spring:
  config:
    activate:
      on-profile: git
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_GIT_URI:file:./config-repo}
          default-label: ${CONFIG_GIT_LABEL:main}
          timeout: 5
          refresh-rate: 60
