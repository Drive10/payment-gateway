spring:
  config:
    activate:
      on-profile: local
  cloud:
    config:
      server:
        native:
          search-locations: ${CONFIG_NATIVE_SEARCH_LOCATIONS:file:./config-repo,file:${user.dir}/config-repo}
