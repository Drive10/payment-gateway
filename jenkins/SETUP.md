#!/bin/bash
# Manual Jenkins Setup Instructions

echo "=========================================="
echo "Jenkins Setup - Manual Steps"
echo "=========================================="
echo ""
echo "Due to CSRF protection, please follow these manual steps:"
echo ""
echo "=========================================="
echo "STEP 1: Create Job"
echo "=========================================="
echo "1. Go to: http://localhost:8080"
echo "2. Login with: admin / Admin@12345"
echo "3. Click 'New Item'"
echo "4. Name: payment-gateway"
echo "5. Select: Pipeline"
echo "6. Check 'This project is parameterized'"
echo "7. Add Choice Parameter:"
echo "   - Name: ENVIRONMENT"
echo "   - Choices: dev, staging, prod"
echo "8. Under Pipeline > Script Path: jenkins/Jenkinsfile"
echo "9. Under Pipeline > SCM: Git"
echo "   - Repository: /Users/macbookair/Documents/GitHub/payment-gateway"
echo "10. Save"
echo ""
echo "=========================================="
echo "STEP 2: Add Credentials"
echo "=========================================="
echo "1. Go to: http://localhost:8080/credentials/store/system"
echo "2. Add Credentials (Secret text):"
echo ""
echo "DEV CREDENTIALS:"
echo "  - ID: db-password-dev, Secret: dev_password"
echo "  - ID: redis-password-dev, Secret: dev_redis_password"
echo "  - ID: jwt-secret-dev, Secret: dev_jwt_secret"
echo "  - ID: vault-token-dev, Secret: dev_vault_token"
echo ""
echo "STAGING CREDENTIALS:"
echo "  - ID: db-password-staging, Secret: staging_password"
echo "  - ID: redis-password-staging, Secret: staging_redis_password"
echo "  - ID: jwt-secret-staging, Secret: staging_jwt_secret"
echo "  - ID: vault-token-staging, Secret: staging_vault_token"
echo ""
echo "PROD CREDENTIALS:"
echo "  - ID: db-password-prod, Secret: prod_password"
echo "  - ID: redis-password-prod, Secret: prod_redis_password"
echo "  - ID: jwt-secret-prod, Secret: prod_jwt_secret"
echo "  - ID: vault-token-prod, Secret: prod_vault_token"
echo "  - ID: k3s-config-prod, Secret: prod_k3s_config"
echo ""
echo "SHARED:"
echo "  - ID: ghcr-token, Secret: github_token"
echo ""
echo "=========================================="
echo "STEP 3: Run Pipeline"
echo "=========================================="
echo "1. Go to: http://localhost:8080/job/payment-gateway/"
echo "2. Click 'Build with Parameters'"
echo "3. Select ENVIRONMENT: dev"
echo "4. Click 'Build'"
echo ""
echo "=========================================="
